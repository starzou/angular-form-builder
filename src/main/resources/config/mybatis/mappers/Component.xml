<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="Component">

	<!-- 根据应用类型获取组件列表 -->
    <select id="getComponentList" parameterType="String" resultType="map">
        select a.code,a.remark from tb_component a inner join tb_component_rel b ON a.component_id = b.component_id where b.is_vaild = 0 and b.type = #{appType} 
    </select>
</mapper>